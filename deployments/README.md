# Deployment artifacts, written by script/Deploy.s.sol.

One file per chain id. These are the handoff to the off-chain allocator service, which
reads addresses from here rather than carrying its own copy.
